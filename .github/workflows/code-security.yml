name: Code quality

on:
    workflow_call:
 

jobs:
    validate: 
        runs-on: ubuntu-latest
        strategy:
            fail-fast: false
            matrix:
                node-version: ["25","26"]

        steps:
          - name: Checkout Code
            uses: actions/checkout@v4

          - name: Setup NodeJs
            uses: actions/setup-node@v4
            with: 
              node-version: ${{matrix.node-version}}

          - name: Install dependencies
            run: npm install

          - name: Install Biome  # for linting
            run: npm install --save-dev @biomejs/biome

          - name: Run Biome
            run: npx @biomejs/biome check .
            
          - name: Run Semgrep  # for security
            uses: returntocorp/semgrep-action@v1
            with:
              config: auto

